apiVersion: v1
kind: Service
metadata:
  name: gateway-service
spec:
  selector:
    app: {{ .Values.app.gateway }}
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000
  type: NodePort